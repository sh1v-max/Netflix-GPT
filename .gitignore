# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.env
*.local
.wrangler

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
# VS Code "Local History" extension — snapshots every saved file,
# including .env, so it must never be tracked (caused a real secret leak,
# see re-do.md).
.history
# Firebase CLI's local hosting cache — machine-specific, not source.
.firebase
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.github

# Claude Code session transcripts — never commit these, they can contain
# the full raw content of everything read/discussed in a chat
*.jsonl
